CS-Center, осень 2014, курс алгоритмов обработки потоковых данных  
Краюшкин Олег
##Реализация алгоритма Мунро-Паттерсона




Реализация алгоритма Мунро-Паттерсона, параметризуемого числом проходов.

1. Постановка задачи
	Реализовать алгоритм Мунро-Паттерсона, параметризуемый числом проходов p.
	Протестировать корректность реализации.

2. Реализация

	Реализован основной метод, имплементирующий целевой алгоритм:  
		`int munro_paterson(int* a, int an, int p, int rank);`

	И две вспомагательные функции:  
		`double log2(double n);`  
		`void merge(std::vector<int> & v1, std::vector<int> &v2, int s);`

3. Тестирование

	В ходе тестирования генерируется некоторое количесво случайных последовательностей, в которых требуется найти элемент ранга rank. Последовательности обрабатываются целевым алгоритмом, результат работы которого проверяется на соответствие результату наивного метода (сортировка всей последовательности, обращение к элементу rank+1).
	Запросы к генерируемым последовательностям подаются в различных комбинациях всех параметров целевого метода (длина последовательсти, количество проходов, ранг искомого элемента)

	Кроме того, в сам метод заложена проверка соответствия количесва используемой памяти относительно сложности алгоритма по памяти.

	Чтобы воспроизвести тесты, запустите bash _run.sh

4. Результаты тестировани
	
	Тесты на корректность ответа пройдены успешно.

5. Дополонительно
	
	Код достаточно подробно откомментирован.
	Кодировка файлов - UTF-8.
